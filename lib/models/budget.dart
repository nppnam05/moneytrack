class Budget {
  int id;
  int user_id;
  int category_id;
  double amount;
  int month;
  int year;
  int created_at;
  
  Budget(this.id, this.user_id, this.category_id, this.amount, this.month, this.year, this.created_at);
}